extends ../layouts/main.pug

block content
    .loginSearch
        input.boardSearch__search(type="text" placeholder="이메일")
        input.boardSearch__search(type="text" placeholder="비밀번호")
    .loginBtn
        button.board__btn 로그인