<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="/styles/login.css")
    body
        .loging
            .login
            .loginSearch
                h2 login
                input.login__search(type="text" placeholder=" ID")
                input.login__search(type="text" placeholder="Pass")
                .loginBtn
                    button.login__btn LOG IN
                        
